
/*
char queen(int x)
{
    return (char) x/2;
}
*/

int main()
{
   /*  int a;  */
   /*  int a, *b, **c;  */
   /*  int blk[5];  */

/*
    ;

   while (a==1) {}

   while (a==1) a = 2;
   while (a==1) { a = 2; }

   while (a==1)
       ;

   do a=1; while(0);
   do { a=1; } while(0);
   do { } while(0);
   do ; while(0);

   for (a=0; a; a++)
    a = 2;

   for (a=0; a; a++)
    { a = 2; }

   for (a=0; a; a++)
    ;

   for (a=0; a; a++)
    {
    }

    if (a==1)
        ;
    else
        ;

    if (a==1) {} else {}

   a = queen(*b,0,0);

    blk[2] = 0;
    a = 0 ? 1 : 2;

    struct foo {
        float a, *b, c;
    };
*/

    struct foo {
        int a, b;
        struct bar {
            float a, b, *c, d, e, f;
            double g, h, **i, j, k, l, m, n;
        } myBar;
    } myFoo, *pMyFoo;


    struct foo2 {
        struct bar2 {
            struct inner1 {
                struct inner2 {
                    float d;
                } myInner2;
            } myInner1;
        } myBar;
    } foos[5];
/*
*/

/*
    struct foo x, *y;

    y = &x;
    x.a = y->b;

    if ( (y->a == x.a ) && y->b == x.b)
        x.a = 0;
*/

/*
   b = &a; 

   b = (int*) &a; 
   c = (int**) &b;
*/

  fish:
   return 0;
}

