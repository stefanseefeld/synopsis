synopsis	:= ../../bin/synopsis -P boost.synopsis

all: BoostPython

BoostPython:
		$(synopsis)

clean:
		rm -rf *~ *.html *.ps *.syn core *.aux *.fn *.cp *.tp *.vr *.log *.ky *.pg \
		       *.fns *.tps *.vrs *.pdf BoostPython

distclean:	clean
